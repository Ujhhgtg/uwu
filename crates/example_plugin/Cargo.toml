[package]
name = "example_plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["dylib"]

[dependencies]
plugin_api = { path = "../plugin_api" }
egui = { version = "*", default-features = false }
